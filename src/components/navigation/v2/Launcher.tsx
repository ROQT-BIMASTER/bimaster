/**
 * Launcher (⌘K) — shim que delega para o novo LauncherDialog premium.
 * Mantido para preservar a API consumida pelo AppRail.
 */
export { LauncherDialog as Launcher } from "./launcher/LauncherDialog";
